<tbody>
    @foreach (var i in FilteredList)
    {
        <AttackDisplay Attack="i" />
    }
    @RenderFragment
</tbody>

@code {
    [Parameter]
    public List<Attack> FilteredList { get; set; }
    [Parameter]
    public RenderFragment RenderFragment { get; set; }

    public void RefreshTable()
    {
        StateHasChanged();
    }
}