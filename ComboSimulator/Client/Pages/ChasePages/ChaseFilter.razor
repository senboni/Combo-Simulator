<tbody>
    @foreach (var i in FilteredList)
    {
        <ChaseDisplay Chase="i" />
    }
    @RenderFragment
</tbody>

@code {
    [Parameter]
    public List<Chase> FilteredList { get; set; }
    [Parameter]
    public RenderFragment RenderFragment { get; set; }

    public void RefreshTable()
    {
        StateHasChanged();
    }
}